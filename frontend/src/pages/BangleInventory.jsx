export default function BangleInventory() {
  return (
    <div className="flex-1 flex items-center justify-center p-8" style={{ background:"var(--bg0)" }}>
      <div className="text-center">
        <div style={{ fontSize:56, marginBottom:16 }}>💍</div>
        <div style={{ fontSize:20, fontWeight:800, color:"var(--ink)", marginBottom:8 }}>
          Bangle Inventory
        </div>
        <div style={{ fontSize:13, color:"var(--ink-faint)" }}>
          Coming soon — variant tracking by colour, size &amp; design
        </div>
      </div>
    </div>
  )
}
